# M0_EXIT_EVIDENCE.md — Milestone 0 Exit Evidence (M0 Step 10)

Governing docs: `docs/M0_IMPLEMENTATION_READINESS_PACKET_v0.1.3.md` §1 (M0 deliverables), §5 (environment verification), §9 (M0 exit gate), §12 (readiness checklist); `CLAUDE.md` §3 (claim-to-test), §4 (branch-first/PR-gated), §7 (scope discipline).

Evidence collected: **2026-07-14** (local, Windows box) by the Implementer (Claude Code). Per `AGENTS.md` §1/§4, this document is evidence only — closing M0 and authorizing Alpha are **owner-only actions**.

---

## 1. Repository state

| Item | Value |
| --- | --- |
| Repo URL | https://github.com/citadelnexus-cnx/harbor-guardians |
| Visibility | public |
| Default branch | `main` |
| `main` HEAD at evidence time | `2c6b4a4d64beb7e0db003c7c68574d7c7989cf14` |
| Working tree at evidence time | clean (`git status --short` → empty, before this file was added) |

## 2. Merged PRs — M0 Steps 1–9

Steps 1–2 (Windows environment preparation; GitHub repo creation) were owner actions per M0 packet §4 ("repo creation, first push … are explicit owner actions") and produced no PR. All subsequent work merged via PR:

| PR | Step | Title | Merge commit |
| --- | --- | --- | --- |
| [#1](https://github.com/citadelnexus-cnx/harbor-guardians/pull/1) | 2.5 | Deliberate permission allow/deny lists (`.claude/settings.json`) | `44f88d2` |
| [#2](https://github.com/citadelnexus-cnx/harbor-guardians/pull/2) | 3 | M0 scaffold (packet area 2) | `41af49b` |
| [#3](https://github.com/citadelnexus-cnx/harbor-guardians/pull/3) | 4 | Docs install + operating rules (CLAUDE.md, AGENTS.md, DECISIONS.md) | `5b5bb7c` |
| [#4](https://github.com/citadelnexus-cnx/harbor-guardians/pull/4) | — | Housekeeping: ignore local M0 transfer kit | `b3e12c0` |
| [#5](https://github.com/citadelnexus-cnx/harbor-guardians/pull/5) | 5 | Schema/data contracts (D39): TS types → generated `/schema`, six GuardianKit seeds + rejected broken fixture | `83d06eb` |
| [#6](https://github.com/citadelnexus-cnx/harbor-guardians/pull/6) | 6 | Sim-harness skeleton (invariant registry + fail-loud runner) | `7d4690b` |
| [#7](https://github.com/citadelnexus-cnx/harbor-guardians/pull/7) | 7 | Save/load skeleton: atomic save (S7) + empty-state round-trip (S5) | `f6d49eb` |
| [#8](https://github.com/citadelnexus-cnx/harbor-guardians/pull/8) | 8 | Tauri 2 app shell: empty smoke-test window | `308e591` |
| [#9](https://github.com/citadelnexus-cnx/harbor-guardians/pull/9) | 9 | CI gate + PR template per M0 packet §4/§8 (DC5) | `0fd284b` |
| [#10](https://github.com/citadelnexus-cnx/harbor-guardians/pull/10) | 9 (proof) | CI gate demonstration (green → red → green) | `2c6b4a4` |

All ten PRs were opened as drafts by the Implementer and promoted/merged by the owner (draft-first rule, `CLAUDE.md` §4).

## 3. Environment / toolchain evidence (M0 packet §5)

Captured 2026-07-14 on the owner's Windows box (Windows 10 Home 10.0.19045, PowerShell):

| Tool | Command | Output |
| --- | --- | --- |
| Node.js | `node -v` | `v22.16.0` |
| pnpm | `pnpm -v` | `10.12.1` (pinned via `package.json` `packageManager`) |
| Git | `git --version` | `git version 2.49.0.windows.1` |
| rustup | `rustup --version` | `rustup 1.29.0 (28d1352db 2026-03-05)` |
| rustc | `rustc --version` | `rustc 1.97.0 (2d8144b78 2026-07-07)` |
| cargo | `cargo --version` | `cargo 1.97.0 (c980f4866 2026-06-30)` |
| MSVC | VS 2022 Build Tools, `VC\Tools\MSVC` | toolset `14.44.35207` (cl.exe product version `14.44.35228.0`, Hostx64/x64) |
| Windows SDK | `Windows Kits\10\Include` | `10.0.26100.0` |
| WebView2 Runtime | registry `EdgeUpdate\Clients\{F3017226-…}` `pv` | `150.0.4078.65` |

## 4. Local verification commands and exit codes

Full suite re-run on 2026-07-14 at `main` = `2c6b4a4`, before this file was written. Every command exited **0**:

| Command | Result | Exit code |
| --- | --- | --- |
| `pnpm exec tsc --noEmit` | typecheck clean | 0 |
| `pnpm run lint` | oxlint clean | 0 |
| `pnpm run schema:validate` | 2 generated schemas match fresh generation (D39 drift guard); 6 guardian seeds valid; `gdn.broken.invalid.json` rejected with 7 schema errors (DC4/DC5) | 0 |
| `pnpm run sim:harness` | 122 invariant IDs registered exactly once; 120 fail-loud stubs threw as required; 2 implemented invariants (S5, S7) passed; determinism proof: two runs at seed 20260714 byte-identical (19904 bytes); persona matrix declared | 0 |
| `pnpm run test:save` | 10/10 tests pass (S7 atomic pipeline, canonical serialization, crash/torn-write/invalid-blob protection, S5 empty-state round-trip) | 0 |
| `cargo check --locked` (in `src-tauri/`) | Tauri 2 shell compiles clean (`Finished dev profile`) | 0 |

Re-running the harness after the suite left the working tree clean — `sim-harness/reports/latest-report.json` regenerated byte-identical to the committed copy, an additional determinism confirmation.

**`pnpm tauri dev` (visual/manual):** the empty smoke-test window launches on the owner's Windows box — confirmed manually by the owner during M0 Step 8 (PR #8). This is an owner attestation, not a headless-reproducible check; CI covers the compile path via `cargo check --locked`.

## 5. CI evidence (M0 packet §4)

- **PR #9 (CI gate introduction):** all `ci` check runs on the PR concluded `SUCCESS` (e.g. [run 29380318325](https://github.com/citadelnexus-cnx/harbor-guardians/actions/runs/29380318325)).
- **PR #10 green → red → green proof**, per-commit `ci` conclusions from the GitHub Checks API:

  | Commit | Content | `ci` conclusion |
  | --- | --- | --- |
  | `503130c` | trivial green change (README CI badge) | success |
  | `3c28cdf` | DELIBERATE RED — broken `gdn.raxa` seed | **failure** (schema validation blocked, DC5) |
  | `75ea3c5` | revert of the deliberate break | success |

  While the red commit was PR head, GitHub reported the required `ci` check as failing and **merge was blocked**; after the revert the check returned green and the owner merged.
- **Branch protection:** ruleset `main-protection` (id 18895700) is **active** on the default branch with rules: `deletion` restricted, `non_fast_forward` (force-push) blocked, `pull_request` required, and `required_status_checks` requiring context **`ci`** (GitHub Actions, integration 15368). Enforcement is server-side/mechanical, not merely procedural.

## 6. Scope evidence (CLAUDE.md §7)

- **No gameplay implementation.** `/src/sim` contains no gameplay logic; all 120 unimplemented invariants are fail-loud stubs — no capability is claimed as passing without proof. The only implemented invariants are the persistence-spine pair S5/S7, which M0 packet §8 explicitly assigns to M0.
- **No Alpha authorization.** Alpha requires the §12 checklist green **and a separate owner authorization** (packet §13 signature options; CLAUDE.md §7). No Alpha work exists in the repo.
- **No deployment.** No packaging, installers, release artifacts, or publish steps exist; CI publishes nothing (`permissions: contents: read`).
- **No secrets.** `.env` is gitignored; no tokens/keys committed; CI reads no secrets.
- **Tauri shell is empty smoke-test only.** `src-tauri` + `src/ui/shell` open one empty window; no game UI.

## 7. Known limitations / deferred items

1. **Real gameplay sim is FUTURE BUILD.** The 120 fail-loud invariant stubs (E, L, M, C, OPS, CARGO, TD, A11Y, DC-runtime, OB, GEAR, W, FCT, GDN suites) await Alpha+ features; each feature PR must turn its stubs green (claim-to-test, CLAUDE.md §3).
2. **S7 reward-duplication portion is future.** S7's atomic-save/no-loss mechanics are proven at M0 for the empty state; the no-reward-duplication aspect (D19 pending-reward block) cannot be exercised until rewards exist (Alpha+). CURRENT BUILD: atomic save + rollback proven; FUTURE BUILD: reward-duplication proof.
3. **Alpha is a separate approval.** Nothing in this document authorizes Alpha; that is a deliberate, separate owner authorization after the M0 exit gate (packet §13 independent-review recommendation).
4. **Production build is not authorized.** No `tauri build`/installer/packaging work is in scope; production-candidate criteria are defined in packet §9 for a much later gate.
5. **Guardian animal canon** — only the flagship trio (Nova/Tarin/Raxa) is approved; Bru/Sef/Dagg animals are working placeholders (G-D6). Schema/sim work is animal-agnostic; lock before Beta content.

## 8. M0 readiness checklist (packet §12) — evidence mapping

```
[x] Area 1  M0 setup plan approved — packet v0.1.3 + signed §13 owner authorization record (held privately by owner)
[x] Area 2  Repo scaffold created, typechecks, CI green — PR #2; §4 above (tsc exit 0; CI green on main)
[x] Area 3  CLAUDE.md + AGENTS.md committed, encode operating rules — PR #3
[x] Area 4  main protected, PR template, CI on push, test PR proves the gate — §5 above (ruleset active; PR #9/#10)
[x] Area 5  Windows env verified; empty Tauri window launches; harness runs headless — §3/§4 above (owner attestation for the window)
[x] Area 6  /docs populated, DECISIONS.md seeded, doc→code citation pattern shown — PR #3; module headers in /src, /data
[x] Area 7  TS types → JSON Schema generated; 6 fixtures validate; broken fixture rejected; DC1–DC6 wired — PR #5; §4 above
[x] Area 8  Harness headless on fixed seeds; 122 invariant IDs registered/fail-loud; save/load round-trips empty state atomically — PRs #6/#7; §4 above
[x] Area 9  Alpha/Beta/Production gate definitions recorded — packet §9 committed in /docs
[x] Owner   Machine, GitHub destination (citadelnexus-cnx/harbor-guardians), package manager (pnpm) confirmed
```

## 9. Final M0 exit verdict

**PASS (Implementer assessment) — Step 10 evidence supports closing Milestone 0.**

All seven M0 deliverables from packet §1 exist and pass their checks; the §12 checklist is fully green per §8 above; every local verification command exits 0; CI is green on `main` and the red-blocks-merge property is proven server-side. No scope breaches were found.

Two items are owner attestations rather than headless-reproducible checks and are flagged for the owner's review before closing: (a) the `pnpm tauri dev` visual window launch (§4), and (b) the signed §13 authorization record held privately outside the repo (§8, Area 1).

**Per `AGENTS.md`, the Implementer does not close gates.** The M0 exit gate closes only by owner approval of this evidence; Alpha begins only with a separate owner authorization. Until then, scope remains **Milestone 0 only**.
