# /data/world

**One responsibility:** world seeds — regions, harbors, routes, nodes — per the World Atlas Foundation (B2), enforcing its enums (region, HarborType) and bounds (e.g. `regions_crossed ∈ 0..5`, W1).
