// Module marker: tsc fails with TS18003 when `include` matches no files, so the
// empty M0 scaffold needs one compilation unit to typecheck. Not gameplay code;
// superseded as real modules land from Step 5 onward.
export {};
