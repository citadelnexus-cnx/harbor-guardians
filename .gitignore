node_modules/
dist/
target/

# Tauri build-time generated schemas (regenerated on every build)
src-tauri/gen/

# Harness batch reports are evidence artifacts, attached to PRs — not committed
sim-harness/reports/
.env
*.local

# Local transfer/staging kit; docs are installed under /docs
_kit/

# Local audit evidence report (read-only audit output, not doctrine)
AUDIT_EVIDENCE.md
