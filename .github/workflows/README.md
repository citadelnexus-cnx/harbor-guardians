# /.github/workflows

**One responsibility:** CI pipelines. On every push: typecheck → lint → schema validation (including broken-fixture rejection) → sim-harness smoke. Red CI blocks merge.

Workflow files are added at M0 Step 9; this folder is reserved by the scaffold.
