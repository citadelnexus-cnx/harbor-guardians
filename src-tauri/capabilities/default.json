{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M0 shell: default core capability for the main window only. No plugin permissions — the shell exposes nothing to the frontend (CLAUDE.md §7).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
