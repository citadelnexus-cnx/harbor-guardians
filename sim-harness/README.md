# /sim-harness

**One responsibility:** the headless CLI runner over the pure sim core — persona matrix plus the claim-to-test invariant suites (SIM_HARNESS_ACCEPTANCE_SPEC v0.6.2).

Fixed seeds → byte-identical repeat runs. Every invariant ID (E, L, M/UX, C, S, OPS, CARGO, TD, A11Y, DC, OB, GEAR, W, FCT, GDN) is registered here as an addressable, fail-loud stub until its feature turns it green — "claimed but untested" is impossible.
